'use client';

import { BasicHero } from '@kit/ui/basic-hero';

export const FaqHero = () => {
  return (
    <BasicHero
      title="FAQ"
      description="よくある質問と回答をまとめています。"
      pattern="dots"
      size="lg"
    />
  );
};
